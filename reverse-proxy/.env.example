# Traefik Reverse Proxy Configuration
# Copy this file to .env and update with your values

# Your domain name (e.g., example.com)
DOMAIN=example.com

# Cloudflare API credentials for automatic DNS challenge
# Get these from: https://dash.cloudflare.com/profile/api-tokens
CF_API_EMAIL=your-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-api-token

# Traefik Dashboard Authentication
# Generate with: echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g
# Or use: docker run --rm httpd:alpine htpasswd -nbB admin your-password | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$your-hashed-password
