# WireGuard VPN Server Configuration
# Copy this file to .env and update with your values

# Domain configuration
DOMAIN=example.com

# User/Group IDs (run 'id' command to get yours)
PUID=1000
PGID=1000

# Timezone (e.g., America/New_York, Europe/London)
TZ=America/New_York

# Server configuration
# Your public IP or domain name
SERVER_URL=vpn.example.com

# WireGuard listen port (default: 51820)
SERVER_PORT=51820

# Number of peers/clients to generate configs for
# You can add more later through the UI
PEERS=5

# DNS server for VPN clients (use Pi-hole for ad-blocking on VPN)
PEERDNS=1.1.1.1

# Internal VPN subnet
INTERNAL_SUBNET=10.13.13.0

# Allowed IPs for routing
# 0.0.0.0/0 = route all traffic through VPN
# 192.168.1.0/24 = only route LAN traffic through VPN (split tunnel)
ALLOWEDIPS=0.0.0.0/0

# WireGuard UI credentials
UI_USERNAME=admin
UI_PASSWORD=change-this-secure-password
UI_SESSION_SECRET=change-this-random-string

# Service Access:
# WireGuard UI: https://vpn.example.com
#
# Port Forwarding Required:
# Forward port 51820/UDP on your router to this server
#
# Client Setup:
# 1. Access the UI at https://vpn.example.com
# 2. Download client config or scan QR code
# 3. Install WireGuard client on your device
# 4. Import the configuration
# 5. Connect!
