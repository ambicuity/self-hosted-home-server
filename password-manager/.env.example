# Vaultwarden Password Manager Configuration
# Copy this file to .env and update with your values

# Domain configuration
DOMAIN=example.com

# Admin token - Generate with: openssl rand -base64 48
ADMIN_TOKEN=change-this-to-random-string

# Allow new user signups (true/false)
SIGNUPS_ALLOWED=true

# Allow invited users to signup (true/false)
INVITATIONS_ALLOWED=true

# SMTP Configuration for email (optional but recommended)
SMTP_HOST=smtp.gmail.com
SMTP_FROM=vaultwarden@example.com
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Service Access URL:
# Vaultwarden: https://vault.example.com
# Admin Panel: https://vault.example.com/admin (use ADMIN_TOKEN)
#
# Getting Started:
# 1. Access https://vault.example.com
# 2. Create your account (if signups are enabled)
# 3. Download Bitwarden client apps for your devices
# 4. Configure them to use your server URL
# 5. After initial setup, consider setting SIGNUPS_ALLOWED=false
