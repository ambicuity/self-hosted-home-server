# CI/CD Stack Configuration
# Copy this file to .env and update with your values

# Domain configuration
DOMAIN=example.com

# User/Group IDs (run 'id' command to get yours)
PUID=1000
PGID=1000

# Gitea Configuration
GITEA_DB_PASSWORD=change-this-gitea-db-password

# Drone Configuration
# After setting up Gitea, create an OAuth2 application in Gitea settings
# Copy the Client ID and Client Secret here
DRONE_GITEA_CLIENT_ID=your-gitea-oauth-client-id
DRONE_GITEA_CLIENT_SECRET=your-gitea-oauth-client-secret

# Drone RPC Secret - Generate with: openssl rand -hex 16
DRONE_RPC_SECRET=change-this-random-hex-string

# Drone admin user (must match your Gitea username)
DRONE_ADMIN_USER=admin

# Service Access URLs:
# Gitea:   https://git.example.com
# Drone:   https://drone.example.com
# Jenkins: https://jenkins.example.com
#
# Setup Steps:
# 1. Start Gitea, create admin account
# 2. In Gitea, go to Settings > Applications > Manage OAuth2 Applications
# 3. Create new OAuth2 app:
#    - Application Name: Drone
#    - Redirect URI: https://drone.example.com/login
# 4. Copy Client ID and Secret to this file
# 5. Restart Drone service
# 6. For Jenkins, get initial admin password:
#    docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
