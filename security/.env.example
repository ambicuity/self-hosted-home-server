# Security Services Configuration
# Copy this file to .env and update with your values

# Domain configuration
DOMAIN=example.com

# Timezone (e.g., America/New_York, Europe/London)
TZ=America/New_York

# Group ID for file permissions
GID=1000

# CrowdSec API Key - Generate after first start:
# docker exec crowdsec cscli bouncers add traefik-bouncer
CROWDSEC_API_KEY=your-crowdsec-api-key

# Wazuh Manager IP (optional - if you have a Wazuh server)
WAZUH_MANAGER=

# Service Access URLs:
# Authelia: https://auth.example.com
#
# Security Features:
# - Authelia: Single Sign-On (SSO) with 2FA for all services
# - Fail2ban: Automatically bans IPs with failed login attempts
# - CrowdSec: Collaborative security with community threat intelligence
# - Wazuh: Security monitoring and compliance
#
# Setup Notes:
# 1. Authelia requires configuration file at authelia-config/configuration.yml
# 2. Generate CrowdSec bouncer API key after first start
# 3. Fail2ban monitors system logs automatically
# 4. Configure Traefik middlewares to use Authelia for protected services
#
# Example Traefik middleware usage:
# Add to service labels: "traefik.http.routers.myservice.middlewares=authelia@docker"
