# Development Tools Configuration
# Copy this file to .env and update with your values

# Domain configuration
DOMAIN=example.com

# User/Group IDs (run 'id' command to get yours)
PUID=1000
PGID=1000

# Timezone (e.g., America/New_York, Europe/London)
TZ=America/New_York

# Code Server Configuration
CODE_SERVER_PASSWORD=change-this-password
CODE_SERVER_SUDO_PASSWORD=change-this-sudo-password
PROJECT_PATH=/home/user/projects

# Development Database
POSTGRES_DEV_PASSWORD=change-this-dev-password

# Service Access URLs:
# Code Server:  https://code.example.com
# Registry:     https://registry.example.com
# Registry UI:  https://registry-ui.example.com
# DB Admin:     https://db-admin.example.com
#
# Setting up Docker Registry authentication:
# 1. Create auth directory: mkdir -p registry/auth
# 2. Generate htpasswd: docker run --rm httpd:alpine htpasswd -Bbn username password > registry/auth/htpasswd
# 3. Use these credentials when pushing/pulling images
#
# Using the Registry:
# docker login registry.example.com
# docker tag myimage:latest registry.example.com/myimage:latest
# docker push registry.example.com/myimage:latest
